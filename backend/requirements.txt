# Master requirements file — includes all dependency groups.
# For Docker, each group is installed as a separate layer for better caching.
# For local dev: pip install -r requirements.txt

-r requirements-base.txt
-r requirements-geo.txt
-r requirements-ml.txt
